//big ass moving codes
move_and_collide((keyboard_check(vk_right) - keyboard_check(vk_left)) * spd, (keyboard_check(vk_down) - keyboard_check(vk_up)) * spd, RunningGuy);

if bullet < 0 {
	bullet = 0
}


	
